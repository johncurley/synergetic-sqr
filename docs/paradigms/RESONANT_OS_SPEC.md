# Resonant Operating System: Architectural Specification (v0.1-Alpha)
## Objective: A Computing Environment for Biological Integrity.

The Resonant OS (working title: **Laminar Shell**) rejects the "Paper-on-Desk" metaphor of traditional computing. It treats the display not as a flat grid of pixels, but as a **Resonant Manifold** synchronized to the user's nervous system.

---

## Pillar I: The Laminar coordinate System (LCS)

In a Resonant OS, the X/Y Cartesian grid is relegated to the Hardware Abstraction Layer (HAL). The user and applications operate in **Isotropic Space**.

*   **Grid Basis:** All window positioning and movement occur on the three 60-degree axes of the Isotropic Vector Matrix (IVM).
*   **Geometric Snapping:** When moving an object, it does not "glide" over pixels; it "flows" between IVM nodes. This creates a tactile, predictive feel that reduces the cognitive load of placement.
*   **The Desktop "Breath":** The underlying coordinate system is dynamic. It pulses gently at a sub-perceptual 61.44 kHz sub-harmonic, preventing the eye from ever "locking" into a static, stressful focus state.

## Pillar II: Isotropic Typography & iconography

Traditional fonts are "Cubic" by design—optimized for 90-degree grids. Laminar Shell utilizes **Energy-Field Typography**.

*   **Gaussian Sampling:** Text is not rendered as hard-edged pixels. Every glyph is a series of vector-curves sampled as an energy field (mimicking the "Mode 9" stability). This makes text look "etched in light."
*   **60-Degree Glyphs:** We are developing a "Sovereign Typeface" where the primary stems and terminals align with the IVM axes, minimizing the "Knot Paradox" at the source.

## Pillar III: Proprioceptive Window Management

Windows are not static boxes; they are **Tensegrity Nodes**.

*   **Topological Displacement:** Opening a new window does not just cover existing ones. It exerts "Laminar Pressure," pushing surrounding windows aside along the IVM vectors to maintain visibility and balance.
*   **Variable Tension:** The borders of active windows vibrate with a subtle "High-Dim" glow. The frequency of this glow indicates the process priority or CPU load, allowing the user to "feel" the system's heartbeat.

## Pillar IV: The Biological Grounding Layer

The OS includes a hardware-level **"Grounding Daemon"** that monitors the user's focus time.

*   **Saccadic Reset:** Every 20 minutes, the OS background (an infinite Mode 9 lattice) gently intensifies while active windows fade. The central **Sovereign Anchor** (Cross) pulses, guiding the user through a 15-second visual reset.
*   **The Piranha Sync:** If hardware-connected (via FT232H), the OS pulses an external LED in phase-lock with the screen's refresh cycle, creating a cross-modal anchor for the observer.

---

## The "Mode 9" Mandate (Visual Standard)

As proven in the SPU-13 Emulator, **Gaussian Energy Sampling** is the canonical method for displaying 60-degree truth on 90-degree hardware. 

1.  **Avoid Jitter:** In software emulators, temporal jitter is limited by the refresh rate, causing "wiggle."
2.  **Use Energy Fields:** Distribute line intensity using a Gaussian profile to "fill" the knots, providing a stable, high-contrast image that the brain can resolve effortlessly.

---

*"The OS should not be a tool you look at; it should be an environment you rest in."*
